+ Faire fonctionner l'affichage et le highlight pour plusieurs couches de chunk
+ Ajouter des shaders basiques sur les blocks

+ changer le block highlight pour ne pas faire double appel à getblock pour CHAQUE block
    => 3ms à gagner
    => ~diff entre 40 et 50 fps si rien n'est appelé
+ peut être faire une fonction render_basic_chunk qui ne prend ni en compte le highlight ni les shaders
    => potentiellement bons gains de fps

+ séparer le calcul des is_bloc_visible de celui des screen coord
+ multithreader le calcul des block_visible et des screen coord (en fonction du plus gourmant en priorité)
  fix le pb qui fait déconner l'affichage des chunks et des blocks quand la scale est pas en mode 0,5/0.25 ect

+ régler PLEINS de bugs :
    crash randoms
    on peu plus bouger la cam ni supprimer des blocks si on dezoom/zoom d'abord
        => le premier mousevent de la liste input bug le reste s'il est éxécuté en premier, bug de SDL apparement


+ implémenter un thread pool pour le multithreader
+ améliorer le hilight block pour supporter le building
  implémenter les outils de construction
- fix les memory leak

+ tester avec des textures en 256
+ adapter à une taille en 256
+ fix le mode panorama où la target n'est pas scale en même temps quand on fait espace


Note cool : donner une scale différente à des blocks permet de donner un effet de nuages


+ rajouter la fonction qui calcul l'ombre d'un block, pck elles ne se refresh pas très bien
    => pk pas juste faire une fonction qui refresh toute une ligne du monde

- ajouter une demi-ombre diagonale pour le côté gauche
